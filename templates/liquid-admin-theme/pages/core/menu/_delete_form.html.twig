<form method="post" action="{{ path('admin_menu_delete', {'id': menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this menu?');">
    <input type="hidden" name="token" value="{{ csrf_token('delete-menu') }}">
    <button class="btn -danger">Delete</button>
</form>
