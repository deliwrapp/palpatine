<form method="post" action="{{ path('admin_menu_delete', {'id': page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this menu?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-menu') }}">
    <button class="btn -danger">Delete</button>
</form>
